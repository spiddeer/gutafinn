{
  "name": "gutafinn-backend",
  "version": "1.0.0",
  "private": true,
  "description": "API backend for Gutafinn – serves places from SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js",
    "import:osm": "node import-osm.js",
    "test": "DB_PATH=:memory: node --test test/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.21.0"
  }
}
